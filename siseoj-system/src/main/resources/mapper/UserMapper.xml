<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sise.oj.mapper.UserMapper">
    <select id="selectByUsernameAndPassword" resultType="com.sise.oj.domain.User">
        select * from user_info where username = #{username} and password = #{password}
    </select>
    <select id="selectByUsername" resultType="com.sise.oj.domain.User">
        select * from user_info where username = #{username}
    </select>
</mapper>